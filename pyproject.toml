[project]
name = "pgvector-experiment"
version = "0.1.0"
description = "PostgreSQL pgvector embeddings experiment"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "psycopg2-binary>=2.9.9",
    "openai>=1.0.0",
    "google-generativeai>=0.3.0",
    "python-dotenv>=1.0.0",
]
